# krelix
The inference control plane built for AI agents. Krelix manages your GPU fleet, model storage, and deployment pipeline — and gives agents an API to dynamically load the right model on the right GPU for the job, then eject it when done.
